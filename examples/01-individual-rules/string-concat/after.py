"""Example with string concatenation converted to f-strings."""

def greet(name, age):
    """Greet someone."""
    message = f"Hello {name}, you are {age} years old"
    return message

def format_data(data):
    """Format data."""
    result = f"Data: {data}"
    return result
